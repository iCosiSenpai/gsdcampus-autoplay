# Configurazione Cloudflare Worker per il receiver delle issue.
# Deploy:  cd worker && wrangler deploy
#
# ISSUE_TOKEN è un SECRET (PAT fine-grained del maintainer): impostalo con
#   wrangler secret put ISSUE_TOKEN
# Scope sulla sola repo iCosiSenpai/gsdcampus-autoplay:
#   • Issues: Read and write   (POST /report)
#   • Contents: Read and write (POST /answers → known_answers_public.json)
# NON scriverlo mai qui dentro — wrangler.toml è committato nella repo PUBBLICA.
#
# KEY è una chiave NON-segreta: filtra solo i bot più trivial. È pubblica (sta
# anche in scripts/lib/issue-report.js come DEFAULT_ISSUE_KEY). Per ruotarla:
# cambia il valore qui + in issue-report.js, poi  wrangler deploy + commit.

name = "gsd-issue-report"
main = "issue-receiver.js"
compatibility_date = "2024-09-01"

[vars]
KEY = "gsd-autoplay-report-key-2026-7f3a9c"

# Workers Logs: conserva i console.log (inclusi i [diag]) nella dashboard
# Cloudflare per qualche giorno, consultabili SENZA tenere `wrangler tail` acceso.
# Volume bassissimo (pochi ping per store): resta nel piano free.
[observability]
enabled = true