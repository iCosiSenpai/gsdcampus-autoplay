node_modules
.DS_Store
# log e artefatti runtime (possono contenere URL del corso / dati personali)
logs/
archive/
backups/
debug/
debug/screenshots/*.png
debug/dumps/*.html
debug/exploration/
# dump di lavoro non necessari nel repo condiviso
data/session_state.json
data/need_answer.json
data/storage_state.json
data/pending_quiz_answers.json
data/course_state.json
data/extracted_full.json
data/summary_dump.json
data/quiz-domande.txt
# NOTA: known_answers_public.json è intenzionalmente tracciato da git come banca condivisa
# database membri: tracciato da git (con esplicito consenso del titolare) così i colleghi
# che fanno git clone hanno subito l'elenco membri e la sezione "Chi sei?" funziona senza
# dover importare il CSV. I file -journal/-wal/-shm sono runtime e restano ignorati.
data/members.db-journal
data/members.db-wal
data/members.db-shm
# stato aggregato cross-utente (rigenerato)
data/dashboard.json
# stato personale per-account (cookie compresi)
data/accounts/
# temp indice lista membri (per select interattiva)
data/.members_last_list.json
# dati personali
config.json
config.json.bak
PROMPT-per-altra-AI.md
.autoplay_pid
.ollama_pid
.scheduler_stop
.supervisor_prompt.txt
# stato hash dipendenze (rigenerato localmente da setup.sh)
.package_hash
.claude/
.claude/plan.md

